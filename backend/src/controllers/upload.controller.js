// TODO Day 16 — YOU write this (after AI sets up multer/Cloudinary)
// Functions: cardAttachment, userAvatar, boardCover

const { success } = require('../utils/response');
const cloudinary = require('../config/cloudinary');

module.exports = {};
