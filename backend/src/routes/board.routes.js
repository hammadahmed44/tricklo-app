// TODO Day 5 — YOU wire the routes
const express = require('express');
const router = express.Router();

// POST   /api/boards
// GET    /api/boards/:id
// PUT    /api/boards/:id
// DELETE /api/boards/:id (archive)

module.exports = router;
