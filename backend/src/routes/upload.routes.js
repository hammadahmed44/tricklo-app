// TODO Day 16 — YOU wire the routes (after AI sets up multer middleware)
const express = require('express');
const router = express.Router();

// POST /api/upload/card/:id   — card attachment
// POST /api/upload/avatar     — user profile photo
// POST /api/upload/board/:id  — board cover image

module.exports = router;
