// TODO Day 6 — YOU wire the routes
const express = require('express');
const router = express.Router();

// POST   /api/cards
// GET    /api/cards/:id
// PUT    /api/cards/:id
// DELETE /api/cards/:id
// PUT    /api/cards/:id/move
// POST   /api/cards/:id/comments
// POST   /api/cards/:id/members

module.exports = router;
